services:
  backend:
    ports:
      - "5000:5000"

  frontend:
    ports:
      - "3000:80"

  nginx:
    ports:
      - "8089:80"
      - "8443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl



